#![cfg_attr(not(feature = "std"), no_std)]

//! Product/domain public model layer.
//!
//! The template does not define concrete domain models yet. Add focused modules
//! here only when a real capability needs stable request, response, params,
//! state, or event types shared by core, runtime, sdk, and bindings.
